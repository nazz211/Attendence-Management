services:
<<<<<<< HEAD
  postgres:
    image: postgres:16
    container_name: attendance_postgres
    environment:
      POSTGRES_USER: rishal_user
      POSTGRES_PASSWORD: 12345678
      POSTGRES_DB: rishal_db
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U rishal_user -d rishal_db"]
      interval: 5s
      timeout: 5s
      retries: 10
=======

  db:
    image: postgres:17
    container_name: attendance-docker-db
    restart: unless-stopped

    environment:
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}

    volumes:
      - postgres_data:/var/lib/postgresql/data

    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 10s
      timeout: 5s
      retries: 5

    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"

>>>>>>> 0a56dfc (Update project)

  backend:
    build:
      context: ./backend
<<<<<<< HEAD
    container_name: attendance_backend
    environment:
      DATABASE_URL: postgresql+psycopg2://rishal_user:12345678@postgres:5432/rishal_db
      JWT_SECRET: change-this-to-a-long-random-secret
      JWT_ALGORITHM: HS256
      ACCESS_TOKEN_EXPIRE_MINUTES: 480
      CORS_ORIGINS: http://localhost,http://localhost:5173
    ports:
      - "8000:8000"
    depends_on:
      postgres:
        condition: service_healthy
    restart: unless-stopped

  frontend:
    build:
      context: ./frontend
    container_name: attendance_frontend
    ports:
      - "3000:80"
    depends_on:
      - backend
    restart: unless-stopped

volumes:
  postgres_data:
=======

    container_name: attendance-docker-backend
    restart: unless-stopped

    environment:
      DATABASE_URL: ${DATABASE_URL}
      JWT_SECRET: ${JWT_SECRET}
      JWT_ALGORITHM: ${JWT_ALGORITHM}
      ACCESS_TOKEN_EXPIRE_MINUTES: ${ACCESS_TOKEN_EXPIRE_MINUTES}
      CORS_ORIGINS: ${CORS_ORIGINS}

    depends_on:
      db:
        condition: service_healthy

    ports:
      - "127.0.0.1:8001:8000"

    volumes:
      - uploads_data:/app/uploads

    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"


  frontend:
    build:
      context: ./frontend

    container_name: attendance-docker-frontend
    restart: unless-stopped

    depends_on:
      - backend

    ports:
      - "127.0.0.1:3001:80"

    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"


volumes:
  postgres_data:
  uploads_data:
  
>>>>>>> 0a56dfc (Update project)
